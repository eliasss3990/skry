package app.skry.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de marca skry (fallback cuando no hay dynamic color, < Android 12).
val SkryBlue = Color(0xFF4F9CF9)
val SkryBlueDark = Color(0xFF1B6FD6)
val SkryInk = Color(0xFF0B0F14)
val SkrySurfaceDark = Color(0xFF121821)
val SkrySurfaceLight = Color(0xFFF6F8FB)
